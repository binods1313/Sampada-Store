       <main>
         {/* Section 1: HERO Banner with Background Image */}
         <section className="s-section" style={{ 
           position: 'relative', 
           overflow: 'hidden',
           background: !heroImage ? 'var(--s-dark)' : 'transparent'
         }}>
           {/* Background Image */}
           {heroImage && (
             <div style={{ 
               position: 'absolute', 
               top: 0, 
               left: 0, 
               width: '100%', 
               height: '100%',
               zIndex: 0,
               opacity: imageVisible ? 1 : 0,
               transform: imageVisible ? 'scale(1)' : 'scale(1.05)',
               transition: imageVisible ? 'opacity 1.5s ease-out, transform 1.5s ease-out' : 'none'
             }}>
               <Image 
                 src={urlFor(heroImage).width(1920).height(1080).url()} 
                 alt={heroImage.alt || 'Company hero image'} 
                 fill
                 style={{ 
                   objectFit: 'cover', 
                   objectPosition: 'center top' 
                 }}
                 sizes="100vw"
               />
             </div>
           )}
           
           {/* Gradient Overlay */}
           {heroImage && (
             <div style={{ 
               position: 'absolute', 
               top: 0, 
               left: 0, 
               width: '100%', 
               height: '100%',
               background: isMobile 
                 ? 'linear-gradient(to bottom, rgba(13, 4, 8, 0.7) 0%, rgba(13, 4, 8, 0.5) 100%)'
                 : 'linear-gradient(to right, rgba(13, 4, 8, 0.75) 0%, rgba(13, 4, 8, 0.4) 50%, rgba(13, 4, 8, 0.2) 100%)',
               zIndex: 1
             }}></div>
           )}
           
           {/* Content Container */}
           <div className="s-container" style={{ 
             position: 'relative', 
             zIndex: 2, 
             display: 'flex', 
             alignItems: 'center', 
             minHeight: '60vh',
             textAlign: 'center'
           }}>
             <p className="s-label">{companyInfo?.industry || 'HERITAGE FASHION'}</p>
             <h1 className="s-heading" style={{ 
               fontSize: 'clamp(2.5rem, 6vw, 4rem)', 
               marginBottom: '24px',
               opacity: titleVisible ? 1 : 0,
               transform: titleVisible ? 'translateY(0)' : 'translateY(10px)',
               transition: titleVisible ? 'opacity 0.8s ease-out, transform 0.8s ease-out' : 'none'
             }}>
               {heroTitle || 'Where Heritage Meets Modern Elegance'}
             </h1>
             <p style={{ 
               color: 'var(--s-text-mid)', 
               fontSize: '1.1rem', 
               lineHeight: '1.8', 
               maxWidth: '700px', 
               margin: '0 auto',
               opacity: descriptionVisible ? 1 : 0,
               transform: descriptionVisible ? 'translateY(0)' : 'translateY(10px)',
               transition: descriptionVisible ? 'opacity 0.8s ease-out 0.2s, transform 0.8s ease-out 0.2s' : 'none'
             }}>
               {heroDescription || 'Founded on the principles of timeless design and uncompromising quality.'}
             </p>
             {companyInfo && (
               <div style={{ display: 'flex', gap: '32px', justifyContent: 'center', marginTop: '32px', flexWrap: 'wrap' }}>
                 {companyInfo.foundedYear && (
                   <div style={{ 
                     opacity: statsVisible ? 1 : 0,
                     transform: statsVisible ? 'translateY(0)' : 'translateY(10px)',
                     transition: statsVisible ? 'opacity 0.8s ease-out 0.4s, transform 0.8s ease-out 0.4s' : 'none'
                   }}>
                     <p style={{ color: 'var(--s-gold)', fontSize: '1.8rem', fontFamily: 'var(--s-serif)', fontWeight: '700', margin: '0 0 4px' }}>
                       {companyInfo.foundedYear}
                     </p>
                     <p style={{ color: 'var(--s-text-dim)', fontSize: '0.75rem', textTransform: 'uppercase', letterSpacing: '1.5px' }}>Founded</p>
                   </div>
                 )}
                 {companyInfo.headquarters && (
                   <div style={{ 
                     opacity: statsVisible ? 1 : 0,
                     transform: statsVisible ? 'translateY(0)' : 'translateY(10px)',
                     transition: statsVisible ? 'opacity 0.8s ease-out 0.5s, transform 0.8s ease-out 0.5s' : 'none'
                   }}>
                     <p style={{ color: 'var(--s-gold)', fontSize: '1.2rem', fontFamily: 'var(--s-serif)', fontWeight: '600', margin: '0 0 4px' }}>
                       {companyInfo.headquarters}
                     </p>
                     <p style={{ color: 'var(--s-text-dim)', fontSize: '0.75rem', textTransform: 'uppercase', letterSpacing: '1.5px' }}>Headquarters</p>
                   </div>
                 )}
               </div>
             )}
           </div>
         </section>
