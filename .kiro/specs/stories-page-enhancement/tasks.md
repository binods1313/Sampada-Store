# Tasks — Stories Page Enhancement

- [x] 1. Create `components/spotlight/EchoCanvas.jsx` — velocity-based echo ring canvas overlay
- [x] 2. Create `components/spotlight/SpotlightReveal.jsx` — lerp-smoothed spotlight reveal hero
- [x] 3. Create `components/stories/SelectedWorksGallery.jsx` — masonry gallery with filter tabs
- [x] 4. Create `components/stories/SelectedWorksGallery.module.css` — gallery styles
- [x] 5. Update `pages/stories/index.js` — replace hero with SpotlightReveal, append SelectedWorksGallery
